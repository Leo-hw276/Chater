const login=document.querySelector("#login");if(login){login.querySelector(".loginbox").innerHTML='<div class="brand" style="justify-content:center"><div class="mark">C</div>Chater</div><h1 id="authTitle">欢迎回来</h1><p id="authHint">登录你的 AI 工作空间</p><div style="display:flex;gap:8px;margin:18px 0"><button class="opt on" id="tabLogin">登录</button><button class="opt" id="tabRegister">注册</button></div><input id="authEmail" type="email" placeholder="邮箱地址"><input id="authName" placeholder="昵称（注册时填写）" style="display:none"><input id="authPassword" type="password" placeholder="密码"><div style="display:flex;gap:8px;align-items:center;margin:10px 0"><canvas id="captchaCanvas" width="130" height="42" style="border-radius:7px;background:#292a2d;cursor:pointer"></canvas><input id="captchaValue" placeholder="图形验证码" style="margin:0"></div><input id="honey" tabindex="-1" autocomplete="off" aria-hidden="true" style="display:none"><button class="primary" id="authSubmit">进入 Chater</button><p id="authError" style="font-size:12px;margin-top:12px;color:#ff8b8b"></p><p style="font-size:12px;margin-top:18px">邮箱只做格式校验；图形验证码用于人机验证</p>';let mode="login",captchaId="",captchaText="";const $=s=>document.querySelector(s),canvas=$("#captchaCanvas"),ctx=canvas.getContext("2d");function refreshCaptcha(){fetch("/api/auth/captcha").then(r=>r.json()).then(c=>{captchaId=c.id;captchaText=c.value;ctx.clearRect(0,0,130,42);ctx.fillStyle="#292a2d";ctx.fillRect(0,0,130,42);ctx.font="bold 22px monospace";ctx.fillStyle="#f2f2f2";ctx.fillText(c.value,12,29)})}function setMode(next){mode=next;$("#tabLogin").classList.toggle("on",mode==="login");$("#tabRegister").classList.toggle("on",mode==="register");$("#authTitle").textContent=mode==="login"?"欢迎回来":"创建 Chater 账户";$("#authHint").textContent=mode==="login"?"登录你的 AI 工作空间":"使用邮箱格式注册";$("#authName").style.display=mode==="register"?"block":"none";$("#authSubmit").textContent=mode==="login"?"进入 Chater":"注册并进入";$("#authError").textContent="";refreshCaptcha()}$("#tabLogin").onclick=()=>setMode("login");$("#tabRegister").onclick=()=>setMode("register");canvas.onclick=refreshCaptcha;refreshCaptcha();$("#authSubmit").addEventListener("click",async e=>{e.stopImmediatePropagation();const payload={email:$("#authEmail").value,password:$("#authPassword").value,name:$("#authName").value,captchaId,captchaValue:$("#captchaValue").value,honey:$("#honey").value};const r=await fetch(mode==="login"?"/api/auth/login":"/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const d=await r.json();if(!r.ok){$("#authError").textContent=d.error||"验证失败";refreshCaptcha();return}localStorage.chaterUser=JSON.stringify(d.user);location.reload()},true)}

